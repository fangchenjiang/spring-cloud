server:
  port: 83
spring:
  application:
    name: xf-consumer-83
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'
